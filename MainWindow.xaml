<Window x:Class="MorseCodeEncoder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Morse Code Encoder" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox x:Name="inputTextBox" Grid.Row="0" Margin="10" AcceptsReturn="True" />
        <TextBox x:Name="outputTextBox" Grid.Row="3" Margin="10" AcceptsReturn="True" />
        <Button x:Name="encodeButton" Grid.Row="1" Margin="10" Content="Encode" Click="encodeButton_Click" />
        <Button x:Name="decodeButton" Grid.Row="2" Margin="10" Content="Decode" Click="decodeButton_Click" />
    </Grid>
</Window>